[build]
builder = "DOCKERFILE"
dockerfilePath = "./dockerfile"
watchPatterns = [
    "requirements.txt",
    "back.py",
    "railway.toml",
    ".env",
    "dockerfile",
    "start.sh",
]

[deploy]
startCommand = "./start.sh"
