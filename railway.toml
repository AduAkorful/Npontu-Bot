[build]
builder = "DOCKERFILE"
dockerfilePath = "./dockerfile"
watchPatterns = [
    "requirements.txt",
    "app/**",
    "railway.toml",
    "dockerfile",
    "start.sh",
]

[deploy]
startCommand = "./start.sh"
