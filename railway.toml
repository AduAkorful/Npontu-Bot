[build]
builder = "DOCKERFILE"
dockerfilePath = "./dockerfile"
watchPatterns = [
    "requirements.txt",
    "app/**",
    "railway.toml",
    "dockerfile",
]

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:${PORT:-5000}"
